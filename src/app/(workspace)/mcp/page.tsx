"use client";

import { PanelRedirectPage } from "@/components/layout/PanelRedirectPage";

/** /mcp — opens the MCP panel in the left sidebar. */
export default function McpPage() {
  return <PanelRedirectPage panel="mcp" />;
}
