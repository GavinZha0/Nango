"use client";

import { PanelRedirectPage } from "@/components/layout/PanelRedirectPage";

/** /artifact — opens the Artifacts panel in the left sidebar. */
export default function ArtifactPage() {
  return <PanelRedirectPage panel="artifact" />;
}
