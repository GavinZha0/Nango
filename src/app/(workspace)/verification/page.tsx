"use client";

import { PanelRedirectPage } from "@/components/layout/PanelRedirectPage";

/** /verification — opens the Verification panel in the left sidebar. */
export default function VerificationPage() {
  return <PanelRedirectPage panel="verification" />;
}
