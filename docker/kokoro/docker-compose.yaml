services:
  kokoro:
    image: ghcr.io/remsky/kokoro-fastapi-cpu:latest
    container_name: kokoro-tts
    ports:
      - "8880:8880"
    environment:
      - PORT=8880
      - KOKORO_API_KEY=my-local-tts-secret
    restart: unless-stopped